##QNA
    - 1️⃣ What is the difference between var, let, and const?
    Ans: const:we can't modify the value ,let: we can modify the value and var:Function-scoped, not block-scoped, Can be redeclared, Can be reassigned, Hoisted and initialized with undefined.
    - 2️⃣ What is the spread operator (...)?
    Ans: makes an array act like single items
    - 3️⃣ What is the difference between map(), filter(), and forEach()?
    ans: map(): transforms each item and returns a new array
    filter(): selects matching items and returns a new array
    forEach() : loops through items but returns nothing
    - 4️⃣ What is an arrow function?
    ans: const hello=()=>console.log('hi);
    - 5️⃣ What are template literals?
    Ans: `hi${id}` this is known as template literals.


